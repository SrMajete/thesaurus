[pytest]
minversion = 7.0
testpaths = tests
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning:pydantic.*
addopts =
    --strict-markers
    --cov=thesaurus
    --cov-report=term-missing
    --cov-report=html:htmlcov
    --cov-branch
